// itouch-shared-framework/src/styles/types.ts

export type Theme = {
  colors: {
    primary: string;
    secondary: string;
    background: string;
  };
};

export type Font={
  fontSize:{
    appHeader: number,
    header: number,
    largeText: number,
    mediumText: number,
    smallText: number
  }
}